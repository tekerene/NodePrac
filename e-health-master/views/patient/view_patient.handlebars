<title>{{title}}</title>

    <div class="container">
   <div class="d-flex">
      {{#each patientsList}}
          <div class="card ml-3">
                <div class="card-header">{{name}}</div>
                <div class="card-body">
                    <div class="d-flex flex-column">
                         <div class="h4">ID: {{id}}</div>
                        <div class="h3">Location: {{location}}</div>

                        <div class="h3">Gender: {{gender}}</div>
                         <div class="h4">Age: {{age}}</div> 
                         
                        
                    </div>
                    
                </div>
               
                
            </div>
         
      {{/each}}
   </div>
</div>